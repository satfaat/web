hello = (val) => "Hello " + val;
const get_char_length = (phrase) => phrase.length;
func_name = function(phrase){return phrase.length;}


const helper_str = {
    "get_size": (phrase) => phrase.length,

}
//helper_str.get_char_length3('how much')
